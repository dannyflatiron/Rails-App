module MessagesHelper

    def index_display_header
    #     if @mission    
    #     "Messages for Mission: #{@mission.title}"
    #     @messages.each do |message| 
    #       "Secret Agent: message.user.name says message.content" 
    #     end 
         
    #    else 
    #     content_tag(:h1, "All Messages")
    #     @messages.each do |m|
    #     "Secret Agent #{m.user.name} says #{m.content} about this mission:  link_to('m.mission.title', mission_path(#{m.mission_id})"
    #     end 
       
      
 

    # @mission.title
    #       end 
       
    end

end
