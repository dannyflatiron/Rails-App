<h1>New Message</h1>


<%= form_for([@message.missions,@message]) do |f| %>

     
  <%= f.label :mission %>
  <%= f.collection_select :mission_id, Mission.all, :id, :title %>

  <%= f.label :Message %>
  <%= f.text_field :content %>


  <%= f.submit "Leave Message" %>

<% end %>
